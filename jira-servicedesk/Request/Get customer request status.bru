meta {
  name: Get customer request status
  type: http
  seq: 23
}

get {
  url: {{baseUrl}}/rest/servicedeskapi/request/:issueIdOrKey/status
  body: none
  auth: inherit
}

params:query {
  ~start: 
  ~limit: 
}

params:path {
  issueIdOrKey: {{issueKey}}
}
