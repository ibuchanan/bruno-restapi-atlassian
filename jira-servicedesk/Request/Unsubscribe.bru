meta {
  name: Unsubscribe
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/rest/servicedeskapi/request/:issueIdOrKey/notification
  body: none
  auth: inherit
}

params:path {
  issueIdOrKey: {{issueKey}}
}
