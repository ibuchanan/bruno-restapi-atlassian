meta {
  name: Get attachment thumbnail
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/rest/servicedeskapi/request/:issueIdOrKey/attachment/:attachmentId/thumbnail
  body: none
  auth: inherit
}

params:path {
  issueIdOrKey: {{issueKey}}
  attachmentId: 
}
