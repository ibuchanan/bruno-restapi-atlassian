meta {
  name: Get sla information
  type: http
  seq: 21
}

get {
  url: {{baseUrl}}/rest/servicedeskapi/request/:issueIdOrKey/sla
  body: none
  auth: inherit
}

params:query {
  ~start:
  ~limit:
}

params:path {
  issueIdOrKey:
}
