meta {
  name: Get request comment by id
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/rest/servicedeskapi/request/:issueIdOrKey/comment/:commentId
  body: none
  auth: inherit
}

params:query {
  ~expand:
}

params:path {
  issueIdOrKey:
  commentId:
}
