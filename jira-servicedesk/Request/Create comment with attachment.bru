meta {
  name: Create comment with attachment
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/rest/servicedeskapi/request/:issueIdOrKey/attachment
  body: json
  auth: inherit
}

params:path {
  issueIdOrKey: {{issueKey}}
}

body:json {
  {
    "additionalComment": "",
    "public": false,
    "temporaryAttachmentIds": []
  }
}
