meta {
  name: Get request type by id
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/rest/servicedeskapi/servicedesk/:serviceDeskId/requesttype/:requestTypeId
  body: none
  auth: inherit
}

params:query {
  ~expand:
}

params:path {
  serviceDeskId:
  requestTypeId:
}
