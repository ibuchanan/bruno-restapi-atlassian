meta {
  name: Attach temporary file
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/rest/servicedeskapi/servicedesk/:serviceDeskId/attachTemporaryFile
  body: multipartForm
  auth: inherit
}

params:path {
  serviceDeskId: {{serviceDeskId}}
}
