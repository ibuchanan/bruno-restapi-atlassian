meta {
  name: Get service desk by id
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/rest/servicedeskapi/servicedesk/:serviceDeskId
  body: none
  auth: inherit
}

params:path {
  serviceDeskId: {{serviceDeskId}}
}
