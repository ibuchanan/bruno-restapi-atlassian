meta {
  name: Get issues in queue
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/rest/servicedeskapi/servicedesk/:serviceDeskId/queue/:queueId/issue
  body: none
  auth: inherit
}

params:query {
  ~start: 
  ~limit: 
}

params:path {
  serviceDeskId: {{serviceDeskId}}
  queueId: 
}
