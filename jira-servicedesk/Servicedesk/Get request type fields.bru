meta {
  name: Get request type fields
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/rest/servicedeskapi/servicedesk/:serviceDeskId/requesttype/:requestTypeId/field
  body: none
  auth: inherit
}

params:query {
  ~expand: 
}

params:path {
  serviceDeskId: {{serviceDeskId}}
  requestTypeId: {{requestTypeId}}
}
