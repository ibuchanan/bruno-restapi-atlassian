meta {
  name: Delete comment
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/rest/api/2/issue/:issueIdOrKey/comment/:id
  body: none
  auth: basic
}

params:path {
  issueIdOrKey: {{issueKey}}
  id:
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
