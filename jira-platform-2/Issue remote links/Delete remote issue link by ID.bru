meta {
  name: Delete remote issue link by ID
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/rest/api/2/issue/:issueIdOrKey/remotelink/:linkId
  body: none
  auth: basic
}

params:path {
  issueIdOrKey: 
  linkId: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
