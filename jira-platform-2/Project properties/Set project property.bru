meta {
  name: Set project property
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/rest/api/2/project/:projectIdOrKey/properties/:propertyKey
  body: json
  auth: basic
}

params:path {
  projectIdOrKey: 
  propertyKey: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
