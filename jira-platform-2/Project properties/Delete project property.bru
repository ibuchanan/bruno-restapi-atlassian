meta {
  name: Delete project property
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/rest/api/2/project/:projectIdOrKey/properties/:propertyKey
  body: none
  auth: basic
}

params:path {
  projectIdOrKey: 
  propertyKey: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
