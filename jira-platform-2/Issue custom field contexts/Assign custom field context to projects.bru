meta {
  name: Assign custom field context to projects
  type: http
  seq: 12
}

put {
  url: {{baseUrl}}/rest/api/2/field/:fieldId/context/:contextId/project
  body: json
  auth: basic
}

params:path {
  fieldId: 
  contextId: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:json {
  {
    "projectIds": []
  }
}
