meta {
  name: Get custom field contexts for projects and issue types
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/rest/api/2/field/:fieldId/context/mapping
  body: json
  auth: basic
}

params:query {
  ~startAt: 
  ~maxResults: 
}

params:path {
  fieldId: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:json {
  {
    "mappings": [
      {
        "issueTypeId": "",
        "projectId": ""
      }
    ]
  }
}
