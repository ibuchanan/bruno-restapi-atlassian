meta {
  name: Convert user identifiers to account IDs in JQL queries
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/rest/api/2/jql/pdcleaner
  body: json
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:json {
  {
    "queryStrings": []
  }
}
