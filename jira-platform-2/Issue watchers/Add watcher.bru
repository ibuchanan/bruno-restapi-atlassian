meta {
  name: Add watcher
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/rest/api/2/issue/:issueIdOrKey/watchers
  body: json
  auth: basic
}

params:path {
  issueIdOrKey: {{issueKey}}
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
