meta {
  name: Remove filter as favorite
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/rest/api/2/filter/:id/favourite
  body: none
  auth: basic
}

params:query {
  ~expand: 
}

params:path {
  id: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
