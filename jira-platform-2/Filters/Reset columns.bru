meta {
  name: Reset columns
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/rest/api/2/filter/:id/columns
  body: none
  auth: basic
}

params:path {
  id: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
