meta {
  name: Deletes a custom project template
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/rest/api/2/project-template/remove-template
  body: none
  auth: basic
}

params:query {
  templateKey: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
