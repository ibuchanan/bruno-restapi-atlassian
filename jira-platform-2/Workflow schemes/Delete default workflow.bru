meta {
  name: Delete default workflow
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/rest/api/2/workflowscheme/:id/default
  body: none
  auth: basic
}

params:query {
  ~updateDraftIfNeeded: 
}

params:path {
  id: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
