meta {
  name: Archive project
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/rest/api/2/project/:projectIdOrKey/archive
  body: none
  auth: basic
}

params:path {
  projectIdOrKey: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
