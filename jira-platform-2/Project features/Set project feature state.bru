meta {
  name: Set project feature state
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/rest/api/2/project/:projectIdOrKey/features/:featureKey
  body: json
  auth: basic
}

params:path {
  projectIdOrKey: 
  featureKey: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:json {
  {
    "state": ""
  }
}
