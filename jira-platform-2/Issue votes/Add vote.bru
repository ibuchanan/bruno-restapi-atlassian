meta {
  name: Add vote
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/rest/api/2/issue/:issueIdOrKey/votes
  body: none
  auth: basic
}

params:path {
  issueIdOrKey: {{issueKey}}
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
