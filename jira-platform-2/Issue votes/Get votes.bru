meta {
  name: Get votes
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/rest/api/2/issue/:issueIdOrKey/votes
  body: none
  auth: basic
}

params:path {
  issueIdOrKey: {{issueKey}}
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
