meta {
  name: Set dashboard item property
  type: http
  seq: 13
}

put {
  url: {{baseUrl}}/rest/api/2/dashboard/:dashboardId/items/:itemId/properties/:propertyKey
  body: json
  auth: basic
}

params:path {
  dashboardId: 
  itemId: 
  propertyKey: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
