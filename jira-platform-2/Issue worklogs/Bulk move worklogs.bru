meta {
  name: Bulk move worklogs
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/rest/api/2/issue/:issueIdOrKey/worklog/move
  body: json
  auth: basic
}

params:query {
  ~adjustEstimate:
  ~overrideEditableFlag:
}

params:path {
  issueIdOrKey: {{issueKey}}
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:json {
  {
    "ids": [],
    "issueIdOrKey": ""
  }
}
