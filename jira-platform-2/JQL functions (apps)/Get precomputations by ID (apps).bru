meta {
  name: Get precomputations by ID (apps)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/rest/api/2/jql/function/computation/search
  body: json
  auth: basic
}

params:query {
  ~orderBy: 
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:json {
  {
    "precomputationIDs": []
  }
}
