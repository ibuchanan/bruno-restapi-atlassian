meta {
  name: Delete an explicit user permission for a repository
  type: http
  seq: 21
}

delete {
  url: {{baseUrl}}/repositories/:workspace/:repo_slug/permissions-config/users/:selected_user_id
  body: none
  auth: none
}
