meta {
  name: List all repository permissions for a workspace
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/workspaces/:workspace/permissions/repositories
  body: none
  auth: none
}

params:query {
  ~q: 
  ~sort: 
}
