meta {
  name: Update the branching model config for a repository
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/repositories/:workspace/:repo_slug/branching-model/settings
  body: none
  auth: none
}
