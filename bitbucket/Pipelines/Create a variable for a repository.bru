meta {
  name: Create a variable for a repository
  type: http
  seq: 38
}

post {
  url: {{baseUrl}}/repositories/:workspace/:repo_slug/pipelines_config/variables
  body: json
  auth: none
}

params:path {
  workspace: 
  repo_slug: 
}
