meta {
  name: Delete a variable for a user
  type: http
  seq: 51
}

delete {
  url: {{baseUrl}}/users/:selected_user/pipelines_config/variables/:variable_uuid
  body: none
  auth: none
}

params:path {
  selected_user: 
  variable_uuid: 
}
