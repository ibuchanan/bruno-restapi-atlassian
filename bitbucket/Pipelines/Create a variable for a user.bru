meta {
  name: Create a variable for a user
  type: http
  seq: 48
}

post {
  url: {{baseUrl}}/users/:selected_user/pipelines_config/variables
  body: json
  auth: none
}

params:path {
  selected_user: 
}
