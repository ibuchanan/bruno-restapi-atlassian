meta {
  name: List steps for a pipeline
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/repositories/:workspace/:repo_slug/pipelines/:pipeline_uuid/steps
  body: none
  auth: none
}

params:path {
  workspace: 
  repo_slug: 
  pipeline_uuid: 
}
