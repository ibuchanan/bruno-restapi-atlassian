meta {
  name: Get issues for epic
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/rest/agile/1.0/epic/:epicIdOrKey/issue
  body: none
  auth: inherit
}

params:query {
  ~startAt:
  ~maxResults:
  ~jql:
  ~validateQuery:
  ~fields:
  ~expand:
}

params:path {
  epicIdOrKey: {{epicKey}}
}
