meta {
  name: Check if data exists for the supplied properties
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/rest/devinfo/0.10/existsByProperties
  body: none
  auth: inherit
}

params:query {
  ~_updateSequenceId:
}

headers {
  Authorization:
}
