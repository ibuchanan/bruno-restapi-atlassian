meta {
  name: Delete a deployment by key
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/rest/deployments/0.1/pipelines/:pipelineId/environments/:environmentId/deployments/:deploymentSequenceNumber
  body: none
  auth: inherit
}

params:query {
  ~_updateSequenceNumber:
}

params:path {
  pipelineId:
  environmentId:
  deploymentSequenceNumber:
}
