meta {
  name: Delete board property
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/rest/agile/1.0/board/:boardId/properties/:propertyKey
  body: none
  auth: inherit
}

params:path {
  boardId: {{boardId}}
  propertyKey:
}
