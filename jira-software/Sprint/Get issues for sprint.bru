meta {
  name: Get issues for sprint
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/rest/agile/1.0/sprint/:sprintId/issue
  body: none
  auth: inherit
}

params:query {
  ~startAt:
  ~maxResults:
  ~jql:
  ~validateQuery:
  ~fields:
  ~expand:
}

params:path {
  sprintId: {{activeSprintId}}
}
