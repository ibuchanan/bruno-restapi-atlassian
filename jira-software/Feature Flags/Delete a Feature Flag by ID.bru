meta {
  name: Delete a Feature Flag by ID
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/rest/featureflags/0.1/flag/:featureFlagId
  body: none
  auth: inherit
}

params:query {
  ~_updateSequenceId:
}

params:path {
  featureFlagId:
}
