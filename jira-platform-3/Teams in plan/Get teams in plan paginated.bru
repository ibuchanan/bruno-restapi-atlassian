meta {
  name: Get teams in plan paginated
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/rest/api/3/plans/plan/:planId/team
  body: none
  auth: inherit
}

params:query {
  ~cursor:
  ~maxResults:
}

params:path {
  planId:
}
