meta {
  name: Delete plan-only team
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/rest/api/3/plans/plan/:planId/team/planonly/:planOnlyTeamId
  body: none
  auth: inherit
}

params:path {
  planId:
  planOnlyTeamId:
}
