meta {
  name: Currently being removed. Search for issues using JQL (GET)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/rest/api/3/search
  body: none
  auth: inherit
}

params:query {
  ~jql:
  ~startAt:
  ~maxResults:
  ~validateQuery:
  ~fields:
  ~expand:
  ~properties:
  ~fieldsByKeys:
  ~failFast:
}
