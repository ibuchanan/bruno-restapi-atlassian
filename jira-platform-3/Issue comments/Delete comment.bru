meta {
  name: Delete comment
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/rest/api/3/issue/:issueIdOrKey/comment/:id
  body: none
  auth: inherit
}

params:path {
  issueIdOrKey: {{issueKey}}
  id:
}
