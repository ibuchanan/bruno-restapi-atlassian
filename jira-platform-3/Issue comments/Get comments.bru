meta {
  name: Get comments
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/rest/api/3/issue/:issueIdOrKey/comment
  body: none
  auth: inherit
}

params:query {
  ~startAt:
  ~maxResults:
  ~orderBy:
  ~expand:
}

params:path {
  issueIdOrKey: {{issueKey}}
}
