meta {
  name: Delete related work
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/rest/api/3/version/:versionId/relatedwork/:relatedWorkId
  body: none
  auth: inherit
}

params:path {
  versionId:
  relatedWorkId:
}
