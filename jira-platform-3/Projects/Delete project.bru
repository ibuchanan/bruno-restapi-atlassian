meta {
  name: Delete project
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/rest/api/3/project/:projectIdOrKey
  body: none
  auth: inherit
}

params:query {
  ~enableUndo:
}

params:path {
  projectIdOrKey:
}
