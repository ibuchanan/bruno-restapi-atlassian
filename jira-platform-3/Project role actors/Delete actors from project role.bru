meta {
  name: Delete actors from project role
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/rest/api/3/project/:projectIdOrKey/role/:id
  body: none
  auth: inherit
}

params:query {
  ~user:
  ~group:
  ~groupId:
}

params:path {
  projectIdOrKey:
  id:
}
