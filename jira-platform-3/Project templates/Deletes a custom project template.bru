meta {
  name: Deletes a custom project template
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/rest/api/3/project-template/remove-template
  body: none
  auth: inherit
}

params:query {
  templateKey:
}
