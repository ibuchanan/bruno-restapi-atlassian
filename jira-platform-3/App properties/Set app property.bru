meta {
  name: Set app property
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/rest/atlassian-connect/1/addons/:addonKey/properties/:propertyKey
  body: json
  auth: inherit
}

params:path {
  addonKey:
  propertyKey:
}
