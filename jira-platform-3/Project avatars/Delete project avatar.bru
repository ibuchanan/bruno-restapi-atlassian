meta {
  name: Delete project avatar
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/rest/api/3/project/:projectIdOrKey/avatar/:id
  body: none
  auth: inherit
}

params:path {
  projectIdOrKey:
  id:
}
