meta {
  name: Get bulk editable fields
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/rest/api/3/bulk/issues/fields
  body: none
  auth: inherit
}

params:query {
  issueIdsOrKeys:
  ~searchText:
  ~endingBefore:
  ~startingAfter:
}
