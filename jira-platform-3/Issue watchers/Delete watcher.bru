meta {
  name: Delete watcher
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/rest/api/3/issue/:issueIdOrKey/watchers
  body: none
  auth: inherit
}

params:query {
  ~username:
  ~accountId:
}

params:path {
  issueIdOrKey:
}
