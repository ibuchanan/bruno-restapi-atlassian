meta {
  name: Set project property
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/rest/api/3/project/:projectIdOrKey/properties/:propertyKey
  body: json
  auth: inherit
}

params:path {
  projectIdOrKey:
  propertyKey:
}
