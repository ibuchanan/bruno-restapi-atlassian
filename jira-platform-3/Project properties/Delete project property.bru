meta {
  name: Delete project property
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/rest/api/3/project/:projectIdOrKey/properties/:propertyKey
  body: none
  auth: inherit
}

params:path {
  projectIdOrKey:
  propertyKey:
}
