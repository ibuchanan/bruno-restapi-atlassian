meta {
  name: Replace custom field options
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/rest/api/3/field/:fieldId/context/:contextId/option/:optionId/issue
  body: none
  auth: inherit
}

params:query {
  ~replaceWith:
  ~jql:
}

params:path {
  fieldId:
  optionId:
  contextId:
}
