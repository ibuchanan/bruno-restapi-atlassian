meta {
  name: Get custom field options (context)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/rest/api/3/field/:fieldId/context/:contextId/option
  body: none
  auth: inherit
}

params:query {
  ~optionId:
  ~onlyOptions:
  ~startAt:
  ~maxResults:
}

params:path {
  fieldId:
  contextId:
}
