meta {
  name: Get project components
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/rest/api/3/project/:projectIdOrKey/components
  body: none
  auth: inherit
}

params:query {
  ~componentSource:
}

params:path {
  projectIdOrKey:
}
