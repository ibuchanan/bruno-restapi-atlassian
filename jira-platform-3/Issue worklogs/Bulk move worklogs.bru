meta {
  name: Bulk move worklogs
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/rest/api/3/issue/:issueIdOrKey/worklog/move
  body: json
  auth: inherit
}

params:query {
  ~adjustEstimate:
  ~overrideEditableFlag:
}

params:path {
  issueIdOrKey: {{issueKey}}
}


body:json {
  {
    "ids": [],
    "issueIdOrKey": ""
  }
}
