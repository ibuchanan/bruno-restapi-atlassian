meta {
  name: Get precomputations (apps)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/rest/api/3/jql/function/computation
  body: none
  auth: inherit
}

params:query {
  ~functionKey:
  ~startAt:
  ~maxResults:
  ~orderBy:
}
