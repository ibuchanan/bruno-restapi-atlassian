meta {
  name: Delete default workflow
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/rest/api/3/workflowscheme/:id/default
  body: none
  auth: inherit
}

params:query {
  ~updateDraftIfNeeded:
}

params:path {
  id:
}
