meta {
  name: Get workflow usages by status
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/rest/api/3/statuses/:statusId/workflowUsages
  body: none
  auth: inherit
}

params:query {
  ~nextPageToken:
  ~maxResults:
}

params:path {
  statusId:
}
