meta {
  name: Set project feature state
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/rest/api/3/project/:projectIdOrKey/features/:featureKey
  body: json
  auth: inherit
}

params:path {
  projectIdOrKey:
  featureKey:
}


body:json {
  {
    "state": ""
  }
}
