meta {
  name: Set user property
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/rest/api/3/user/properties/:propertyKey
  body: json
  auth: inherit
}

params:query {
  ~accountId:
  ~userKey:
  ~username:
}

params:path {
  propertyKey:
}
