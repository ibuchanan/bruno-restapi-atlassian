meta {
  name: Delete user property
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/rest/api/3/user/properties/:propertyKey
  body: none
  auth: inherit
}

params:query {
  ~accountId:
  ~userKey:
  ~username:
}

params:path {
  propertyKey:
}
