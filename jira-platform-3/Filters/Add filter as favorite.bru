meta {
  name: Add filter as favorite
  type: http
  seq: 12
}

put {
  url: {{baseUrl}}/rest/api/3/filter/:id/favourite
  body: none
  auth: inherit
}

params:query {
  ~expand:
}

params:path {
  id:
}
